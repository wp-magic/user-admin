{% if setting.label %}
<p>
  <label for="{{ setting.name }}">{{ setting.label }}</label>
</p>
{% endif %}
<p>
  <textarea cols="100" rows="5" id="{{ setting.name }}" name="{{ setting.name }}">{{ setting.value }}</textarea>
</p>
